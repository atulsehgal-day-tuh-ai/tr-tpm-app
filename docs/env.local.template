# Local environment template (copy to `.env.local`)
#
# NOTE:
# - `.env.local` is already gitignored in this repo.
# - Put DEV credentials here locally.
# - Stage/Prod credentials should be configured in Azure App Service settings (not committed).

# DEV database (Azure Postgres)
# Replace `YOUR_PASSWORD` with the real password (URL-encode special chars).
# Example:
# DATABASE_URL=postgresql://hopefulwigeon3:YOUR_PASSWORD@db-pg-tr-tpm-dev.postgres.database.azure.com:5432/tr_tpm_dev?sslmode=require
DATABASE_URL=

# Azure AD (Entra ID) SPA config (public)
NEXT_PUBLIC_AZURE_AD_CLIENT_ID=
NEXT_PUBLIC_AZURE_AD_TENANT_ID=
NEXT_PUBLIC_AZURE_AD_REDIRECT_URI=http://localhost:3000

# Optional: expected audience for server-side token verification (API access token aud)
# If you expose an API with Application ID URI `api://<clientId>`, you can leave this blank.
AZURE_AD_API_AUDIENCE=

# Admin authorization (choose one approach)
ADMIN_GROUP_OIDS=
ADMIN_EMAILS=

